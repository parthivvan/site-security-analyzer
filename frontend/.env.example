# Frontend environment variables for Vite
# Copy to .env and edit as needed
# If backend runs with `python app.py`, it listens on 127.0.0.1:5000
# If using Docker (backend/Dockerfile), it listens on 127.0.0.1:8080

# Example (Flask dev server):
VITE_API_URL=http://127.0.0.1:5000

# Example (Docker):
# VITE_API_URL=http://127.0.0.1:8080
